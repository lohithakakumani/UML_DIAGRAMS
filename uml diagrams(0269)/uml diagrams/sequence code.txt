sequenceDiagram
    participant User
    participant "Login Page" as LP
    participant "Auth Service" as AS
    participant "Dashboard" as DB

    User ->> LP: Enter Credentials
    LP ->> AS: Validate Credentials
    AS ->> LP: Authentication Result
    LP ->> DB: Redirect if Success
