classDiagram
    class TrafficControlSystem {
        -String systemID
        -String location
        -String status
        +monitorTraffic()
        +controlSignals()
        +detectViolations()
    }
    class TrafficSignal {
        -String signalID
        -String location
        -String currentState
        +changeState()
        +syncWithControlSystem()
    }
    class Vehicle {
        -String vehicleID
        -String licensePlate
        -float speed
        +detectSpeed()
        +registerViolation()
    }
    class TrafficCamera {
        -String cameraID
        -String location
        -bool isActive
        +captureImage()
        +sendDataToControlSystem()
    }
    class TrafficPolice {
        -String officerID
        -String name
        -String assignedArea
        +issueTicket()
        +monitorTraffic()
    }
    class Pedestrian {
        -String pedestrianID
        -int age
        -String location
        +requestCrossing()
        +followSignals()
    }
    class EmergencyVehicle {
        -String priorityLevel
        +requestSignalOverride()
    }
    
    TrafficControlSystem "1" --> "*" TrafficSignal : manages
    TrafficControlSystem "1" --> "*" Vehicle : monitors
    TrafficControlSystem "1" --> "*" Pedestrian : monitors
    TrafficCamera "*" --> "*" Vehicle : captures violations
    TrafficPolice "*" --> "*" Vehicle : issues tickets
    EmergencyVehicle --|> Vehicle : inherits
    EmergencyVehicle "1" --> "1" TrafficControlSystem : requests priority override